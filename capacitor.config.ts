import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.react_capacitor_android_permissions',
  appName: 'react_capacitor_android_permissions',
  webDir: 'build'
};

export default config;
